# 数据集 / 模型权重 / 运行产物 —— 体积大, 不纳入版本库, 避免撑爆磁盘与仓库
# 说明: 原始数据 (图片/标签) 一律不入库, 但**元信息文档与转换配置**必须入库以便 diff/回滚。
#       父目录被整块忽略后 git 不再递归进入, 故必须逐层放行目录再重新忽略其内容。
datasets/**
!datasets/raw/
datasets/raw/**
!datasets/raw/**/
!datasets/raw/**/DATASET_INFO.md
!datasets/processed/
datasets/processed/**
!datasets/processed/data.yaml
models/*.pt
models/*.onnx
models/*.weights
runs/
*.pth
*.ckpt
*.onnx
*.weights

# Python 缓存
__pycache__/
*.pyc
.ipynb_checkpoints/

# 虚拟环境 (隔离 venv 不入库)
venv/
.env/
*.egg-info/

# 操作系统 / 编辑器
Thumbs.db
Desktop.ini
.DS_Store

# 日志
*.log

# 临时克隆 / 中间产物 (嵌套 whale-clone 等, 不入库)
.tmp/

# Ultralytics 运行期配置/字体缓存 (YOLO_CONFIG_DIR 重定向, Phase 13)
.yolo_config/

# AMP 检查等临时下载的权重目录 (Phase 13)
weights/

# GitHub 凭据 (防御: 若误建 .credentials 目录则不入库; 实际凭据内嵌于 .git/config, 见 docs/git_remote.md)
.credentials/
